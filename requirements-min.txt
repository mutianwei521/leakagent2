# Core stack: enough to run the executor-supervisor system, the smoke tests,
# the EXA7 / KY4 / L-Town reproduction legs and every figure script.
# The exact frozen environment used for the paper is in requirements.txt.
wntr==1.4.0
numpy==2.3.5
scipy==1.17.1
pandas==2.3.3
networkx==3.6.1
scikit-learn==1.8.0
matplotlib==3.10.8
shapely==2.1.2
leidenalg==0.11.0
igraph==1.0.0
PyYAML==6.0.3
openpyxl==3.1.5

# opt-in extras:
# ollama==0.6.1            # only for reproduce_all.py --with-llm
# torch, torch-geometric   # only for the trained GCN baseline in eval/baselines_fair.py
